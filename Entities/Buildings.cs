// using Unity.Entities;

// EntityArchetype Plank = entity_manager.CreateArchetype( // Create an archetype
// typeof(Plank),
// typeof(Health),
// typeof(Size)
// );

// EntityArchetype Floor_Board = entity_manager.CreateArchetype( // Create an archetype
// typeof(FloorBoard),
// typeof(Health),
// typeof(Size)
// );